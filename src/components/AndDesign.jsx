import React from 'react';
import { Button } from 'antd';

const AntDesign = () => {
  return (
    <Button type="primary">
      Button with Ant Design
    </Button>
  );
};

export default AntDesign;
